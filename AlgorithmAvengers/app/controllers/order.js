// OrderController:

// Responsibilities:
// Placing orders: Manages the process of placing orders for selected books, including updating order status and calculating total prices.
// Order history: Retrieves and displays the order history for users.
// Actions: placeOrder, viewOrder, viewOrderHistory.
let OrderModel = require("../models/order");
